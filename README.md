* Installs a package that monitors network traffic
* Sets up an email script that sends monthly statistics
* Adds into cron for recurrence

### Installation

```bash
bash Network-Traffic/install.sh
```
